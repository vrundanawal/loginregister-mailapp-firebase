22-02-2023
1) Set up the firebase configuration
2) Learn about the box model in css.

24/02/2023
1)Register the user and store the values in database
2)Service used for this Email and password
3) Login the user deatils and checking from DB.
4) Used Context api and some utility function 
5) added service with google account
6) add the user with google account

27/02/2023
1) to create a new branch : -git checkout -b feature/develope
2) Push into github : - git push origin feature/develope
3) fireStore Database is the database to used to stored the data

4) Add the rules inside the firestore Database
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Listings
    match /listings/{listing} {
    	allow read;
      allow create: if request.auth != null;
    	allow delete: if resource.data.userRef == request.auth.uid;
      allow update: if resource.data.userRef == request.auth.uid;
    }
   
    // Users
    match /users/{user} {
    	allow read;
    	allow create;
    	allow update: if request.auth.uid == user
    }
  }
}

5) Add rules to Storage

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read;
      allow write: if
      request.auth != null 
    }
  }
}

6)Iside the cloudStore :
Data Tab :- Add the collections
collection Id : listings

1-03-2023
1) Cookies and local storage and session storage

2-03-2023
1) Anand went to office. No call.
1) I converted the app using Context-Api


03-03-2023
1) Working on logout functionality
2) Compose mail feature explanation
3)Regular Expression :- need to study
4) BDD :- Test cases

06-03-2023
not feeling well. So take seek leave.

07-03-2023
1)Form validation
npm install react-hook-form
2) Update the code for form validation using react-form library.


08-03-2023
1) Send to functionality is done
2) Added regular expression in registration form
3) To show the success message used react toastify liabraby.

09-03-2023
1) No call : He went to office

10-03-2023
1)Pass the current user to context api.
2) Work on disabled button with form validation
3) Learned about regular expression

11-03-2023
1) Working on logout functionality


